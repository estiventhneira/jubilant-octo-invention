import React from "react";

const TituloDeCarrusel = ({ titulo }) => {
  return (
    <div>
      <h2 className="mi-lista-home">{titulo}</h2>
    </div>
  );
};

export default TituloDeCarrusel;
